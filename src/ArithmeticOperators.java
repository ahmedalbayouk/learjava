public class ArithmeticOperators {
    /*
     basic operators
     * + / %
     ++ --
     it depend on number priority
     small number%bigger number= the small number
     num%the sam num=0
     pyte short= short
     int short = int
     int flout = flout
     flout double = double
     */
    public static void main(String[] args) {
        int x = 9, y=2;
        System.out.println((float) x/y);
    }
}
